# Database
DATABASE_URL="file:./dev.db"

# Authentication
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Session Configuration
SESSION_EXPIRY_DAYS="30"
MAGIC_LINK_EXPIRY_MINUTES="15"

# SMTP Email Configuration
# Leave these blank to use Mailpit for local development
# For production, configure your SMTP provider settings

# SMTP Server Settings
SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""

# Email From Settings
SMTP_FROM_NAME="VoxStudent"
SMTP_FROM_EMAIL="noreply@voxstudent.com"

# Mailpit Settings (for local development when SMTP is not configured)
# Mailpit runs on localhost:1025 for SMTP and localhost:8025 for web interface
# No configuration needed - just run: docker run -d --name mailpit -p 8025:8025 -p 1025:1025 axllent/mailpit

# Admin Configuration
# Super admin and admin emails (comma-separated)
SUPER_ADMIN_EMAIL="luciano@hcktplanet.com"
ADMIN_EMAILS="luciano@hcktplanet.com"

# WhatsApp Magic Link Configuration
# Super admin and admin phone numbers for WhatsApp login (comma-separated)
SUPER_ADMIN_PHONE="+5511999999999"
ADMIN_PHONES="+5511888888888,+5511777777777"

# Application Settings
NODE_ENV="development"
